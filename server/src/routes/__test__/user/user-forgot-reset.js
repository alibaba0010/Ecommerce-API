import app from "../../../app.js";
import request from "supertest";

// describe("User Forgot/Reset Password Functionality", () => {
//   it("fails to reset password with invalid token", async () => {
//     await request(app)
//       .patch("/v1/resetpassword/invalidtoken")
//       .send({ password: "newpass", confirmPassword: "newpass" })
//       .expect(400);
//   });
// });
