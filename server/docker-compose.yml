version: "2.14.2"

services:
  ecommerce-api:
    container_name: backend
    # image: node:18-alpine3.16
    build: ./
    command: npm run dev
    working_dir: /usr/src/test
    ports:
      - "2000:2000"